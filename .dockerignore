# Git files
.git
.gitignore

# Docker files
Dockerfile
.dockerignore
docker-compose.yml

# Dependencies are installed in the container
node_modules

# Next.js build output, generated within the container
.next

# Local environment variables should not be copied into the image
.env